<h1>Editing Book</h1>
<%= form_with model:@book, local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>
<h4>Body</h4>
<%= f.text_area :body %>
<div></div>
<%= f.submit "Update Book" %>

<span><%= link_to "Show", :back %></span>
<span><%= link_to "Back", "" %></span>

<% end %>